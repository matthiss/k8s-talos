
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: whoami-ingressroute
  namespace: whoami
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`whoami.scheil.one`)
      kind: Rule
      services:
        - name: whoami
          port: 80
